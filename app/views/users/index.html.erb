<div class="container">
    <div class="row">
      
        <%= render "users/info", user: @user, book: @book %>

        <div class="col-xs-9">
          <h2>Users</h2>
          <table class="table">
            <thead>
              <th>image</th>
              <th>name</th>
              <th colspan="3"></th>
            </thead>
            <tbody>
              <% if !@users.nil? %>
                <% @users.each do |user| %>
                  <tr>
                    <td>
                      <%= attachment_image_tag user, :profile_image, size: "50x50", fallback: "no_image-c7305210e2d30bf8f19461ca05a151bba6413a44a35124f673246160efefdc5e.jpg" %>
                    </td>
                    <td>
                      <%= user.name %>
                    </td>
                    <td>
                      <%= link_to "Show", user_path(user) %>
                    </td>
                  </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
  
        </div>
    </div>
</div>
 